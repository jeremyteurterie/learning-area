var pet = "dog";
pet = "cat";
console.log(pet); // cat

// Les variables ne sont pas des valeurs, elles pointent vers des valeurs
// When we change a variable, we simply point to another value

var valeur = 3000;
