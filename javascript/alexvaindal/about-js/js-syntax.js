// le code javascript est execute de facon sequentiel
// chaque ligne correspond a une instruction
// les instructions peuvent etre ecrites sur plusieurs lignes

var num = 5;
num = num * 2;

console.log(num);

// on peut creer des fonctions pour stocker du code

function Hellojs() {
  return x * 2;
}
